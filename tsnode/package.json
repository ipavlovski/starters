{
  "scripts": {
    "tsnd": "/usr/bin/tsnd -r dotenv/config --exit-child --respawn --clear init.ts",
    "repl": "while true; do /usr/bin/ts-node -e 'console.clear(); console.log(\"ready\")' -i; sleep 0; done"
  },
  "dependencies": {
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "@tsconfig/node20": "^20.1.2",
    "@types/node": "^20.10.6",
    "dprint": "^0.45.0",
    "tsx": "^4.7.0"
  }
}
